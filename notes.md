# Syntax:
out | output to console
nil | null / empty value
val | var //unsure
def | define function
ret | return a value
cls | declare class
obj | "this" inside object
ini | if a func has ini() it is the constructor
sup | java's super keyword, access parent
ext | exit programm // todo without book
